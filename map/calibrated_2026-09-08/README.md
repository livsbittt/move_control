# Gazebo 자동 보정·맵핑 결과

완료 기준을 통과한 지도입니다. `map.yaml`과 `map.pgm`을 같은 폴더에 두고 사용합니다. 해상도는 2 cm입니다. 기존 지도 파일은 변경하지 않았습니다.

- 저장 시점: 시뮬레이션 2780.2초, 미확인 영역 표시값 0.0% (소수점 셋째 자리로 반올림한 비율).
- 벽 객체 검출: 48/48, 통로 품질 96.6%, 가짜 장애물 비율 표시값 0.0%.
- 전체 실행의 측정 이동 거리: 22.719 m. 중심–벽 최소 거리: 0.12071 m. 설정 반경: 0.105 m.
- 전후진 및 양방향 회전 보정 후 주행했습니다. 약 640초에 개선한 경로 노드를 재시작한 연속 통합 검증입니다.

`overlay.png`는 저장 지도·원본 미로·중첩 비교입니다. `metrics.json`은 변경되지 않는 지도 스냅샷의 해시와 품질, `full_run_metrics.json`은 전체 실행의 측정 이동 기록, `live_evidence.json`은 최신 보정 프로파일/적용 응답/안전 형상과 단일 모터 명령 발행자를 담습니다. 최종 판정은 `acceptance.json`입니다. `run_manifest.json`은 실행을 기록한 초기 메타데이터이므로 완료 판정에 사용하지 않습니다.

이 결과는 Gazebo VelocityControl 모델의 알고리즘 검증입니다. 카메라·IR은 가상 입력이며, IMU는 Gazebo 위치에서, US는 라이다에서 생성했습니다. 실제 바퀴 구동·접지·독립 센서·실물 로봇의 안전 인증을 입증하지 않습니다. 원래 바퀴 모델의 전진 보정 실패는 별도 제한으로 남아 있습니다.
